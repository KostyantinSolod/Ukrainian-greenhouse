<Window x:Class="Ukrainian_greenhouse.Views.EditCmb"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:Ukrainian_greenhouse.ViewModels"
        xmlns:local="clr-namespace:Ukrainian_greenhouse"
        mc:Ignorable="d"
        Title="EditCmb" Height="450" Width="800">
    <Window.DataContext>
        <vm:EditCmbViewModel/>
    </Window.DataContext>
    <Grid>
        <Grid>
            <ComboBox ItemsSource="{Binding CmbBoxItems}" DisplayMemberPath="Name" SelectedItem="{Binding SelectedCultureItem}" Margin="464,20,58,292" />

            <Button Content="Add" Command="{Binding AddCulture}" Margin="95,156,505,166" />
            <TextBox x:Name="AddCulture" Text="{Binding NameOfCulture, UpdateSourceTrigger=PropertyChanged}" Margin="90,47,505,292"/>

            <Button Content="Delete" Command="{Binding DeleteCulture}" Margin="516,156,85,166" />

        </Grid>
    </Grid>
</Window>
